# Agent Instructions

- **CI**: Run `bun run ci` after completing a meaningful task/phase. Fix any
  failures before proceeding.
- **Dependencies**: Use `bun add` or `bun add -d` to manage dependencies. Do not
  edit `package.json` manually for dependencies.
- **Runtime**: Use `bun`.
