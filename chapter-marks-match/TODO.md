# Phase 1 Tasks

Mark that task as done
Now lets try to call whisper-cli, that is the next task.

- [x] Create validate-chapters.sh script with ENV vars AUDIOBOOKS_ROOT and BOOK_DIR
- [x] Parse metadata.json and display chapter bookmarks (id, start time, title)
- [x] Research ffplay start times and add ffplay command to output for each chapter
- [x] Set up whisper transcription for each chapter bookmark segment
- [x] Combine everything into a bash script that processes all chapters

