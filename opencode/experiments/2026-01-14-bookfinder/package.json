{
	"name": "2026-01-14-bookfinder",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"start": "bun run src/index.ts",
		"dev": "bun run --watch src/index.ts",
		"check": "tsc --noEmit",
		"lint": "biome check .",
		"format": "biome check --write .",
		"test": "bun test",
		"ci": "bun run lint && bun run check && bun test",
		"clean": "rm -rf node_modules dist && bun install",
		"outdated": "bun outdated"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.11",
		"@types/bun": "latest",
		"@types/yargs": "^17.0.35"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"yargs": "^18.0.0"
	}
}
