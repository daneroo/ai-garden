scratch/
media/
# Repo-root /books only (leading slash anchors it). Without the slash this
# matched any "books/" dir, e.g. epub-split/inspect/reports/books. Remove this
# line once the top-level /books folder is gone — it appears unused.
/books/
external-repos/
# prosodio is bootstrapped here as a nested repo for visibility, then `mv ..`
# out to a sibling once it is ready for handoff. ai-garden does not track it.
prosodio/
.DS_Store
